name: Algorithm validations

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - uses: actions/cache@v5
        id: cache
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install system dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y \
                        graphviz libgraphviz-dev pkg-config glpk-utils gcc g++ gfortran libopenblas-dev liblapack-dev
      - name: Install Python dependencies
        run: |
          python3 -m pip install -v --upgrade pip
          python3 -m pip install -vv -e .[validation]
      - name: Test with own validation script
        run: |
          python3 tests/validate_algs.py
